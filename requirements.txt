# The site pipeline only. Two dependencies:
#   pandas     — aggregation in scripts/site_data/. PINNED, and CI installs THIS file:
#                pandas changes how .astype(str) treats missing values between minor
#                versions, which once passed locally and failed on the runner. Keep this
#                equal to the version you develop against.
#   pyairtable — the read-only snapshot fetch
# The published site itself has no Python at run time: it is static files plus a
# vendored copy of ECharts.
pandas==2.3.3
pyairtable==3.0.1
